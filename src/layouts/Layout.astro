---
import { SEO } from "astro-seo";
import Footer from "@/components/footer.astro";
import Navbar from "@/components/navbar/navbar.astro";
import InfoBox from "@/components/info-box.astro";

import "../styles/global.css";
import Analytics from '@vercel/analytics/astro';

export interface Props {
  title?: string;
  description?: string;
  showSidebar?: boolean;
  ogImage?: string;
  noindex?: boolean;
}

const { title, description, showSidebar = false, ogImage, noindex = false } = Astro.props;

// src/data/contact.ts lit contact.yaml via fs au chargement du module — cela
// fonctionne au build (pages statiques) mais casse à l'exécution sur les
// pages rendues à la demande (ex. /admin), où le répertoire courant du
// serverless Vercel n'est pas la racine du dépôt (ENOENT). Un import
// statique en tête de fichier s'exécuterait pour TOUTES les pages qui
// utilisent ce layout, y compris les pages noindex (admin) qui n'affichent
// jamais le bloc JSON-LD ci-dessous : on ne l'importe donc que si on va
// réellement l'utiliser, ce qui n'arrive qu'au build pour les pages
// publiques statiques.
let contactJsonLd: { telephone: string[]; email: string } | null = null;
if (!noindex) {
  const { contact, telHref } = await import("@/data/contact");
  contactJsonLd = { telephone: [telHref(contact.mobile), telHref(contact.landline)], email: contact.email };
}
const metaDescription = description ?? "Cours de calligraphie chinoise à Lyon avec Christophe Desmur. Ateliers collectifs au Dojo Zen et Miroirs du Ciel, cours particuliers et interventions en écoles et entreprises.";

const canonicalURL = new URL(Astro.url.pathname, Astro.site).toString();
const resolvedImageWithDomain = new URL(ogImage || "/assets/opengraph.webp", Astro.site).toString();

const siteSuffix = "Calligraphie chinoise Lyon";
const isAdmin = Astro.url.pathname.startsWith('/admin');

let hasAdminSession = false;
if (isAdmin) {
  const { authenticateRequest } = await import("@/lib/admin/auth");
  hasAdminSession = Boolean(authenticateRequest(Astro.request).session);
}

const makeTitle = !title
  ? (isAdmin ? "Administration · Ateliers de calligraphie" : "Calligraphie chinoise Lyon — Christophe Desmur")
  : isAdmin
    ? (title.includes("Ateliers de calligraphie") ? title : `${title} · Ateliers de calligraphie`)
    : (title.includes(siteSuffix) || title.includes("Lyon") ? title : `${title} | ${siteSuffix}`);
---

<!doctype html>
<html lang="fr" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.webp" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <meta name="generator" content={Astro.generator} />
    <script is:inline>
      (function () {
        const saved = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (saved === 'dark' || (!saved && prefersDark)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>

    <SEO
      title={makeTitle}
      description={metaDescription}
      canonical={canonicalURL}
      noindex={noindex}
      nofollow={noindex}
      twitter={{
        card: "summary_large_image",
        title: makeTitle,
        description: metaDescription,
        image: resolvedImageWithDomain,
        imageAlt: makeTitle,
      }}
      openGraph={{
        basic: {
          url: canonicalURL,
          type: "website",
          title: makeTitle,
          image: resolvedImageWithDomain,
        },
        image: {
          alt: makeTitle,
        },
        optional: {
          description: metaDescription,
          siteName: "Calligraphie chinoise Lyon — Christophe Desmur",
          locale: "fr_FR",
        },
      }}
    />
    <Analytics />
    {!noindex && (
      <script type="application/ld+json" set:html={JSON.stringify({
        "@context": "https://schema.org",
        "@type": ["LocalBusiness", "EducationalOrganization"],
        "name": "Christophe Desmur — Calligraphie chinoise Lyon",
        "alternateName": "Ateliers de calligraphie chinoise à Lyon",
        "description": "Cours hebdomadaires de calligraphie chinoise, cours particuliers et ateliers sur mesure à Lyon avec Christophe Desmur. Pratique au pinceau, encre de Chine et philosophie.",
        "url": "https://ateliersdecalligraphie.com",
        "telephone": contactJsonLd!.telephone,
        "email": contactJsonLd!.email,
        "image": "https://ateliersdecalligraphie.com/assets/opengraph.webp",
        "priceRange": "€€",
        "founder": {
          "@type": "Person",
          "name": "Christophe Desmur",
          "url": "https://ateliersdecalligraphie.com/le-calligraphe"
        },
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "11, rue Dumenge",
        "postalCode": "69004",
        "addressLocality": "Lyon",
        "addressRegion": "Auvergne-Rhône-Alpes",
        "addressCountry": "FR"
      },
      "geo": {
        "@type": "GeoCoordinates",
        "latitude": 45.776853,
        "longitude": 4.831559
      },
      "areaServed": [
        { "@type": "City", "name": "Lyon" },
        { "@type": "AdministrativeArea", "name": "Métropole de Lyon" },
        { "@type": "AdministrativeArea", "name": "Rhône" }
      ],
      "openingHoursSpecification": [
        {
          "@type": "OpeningHoursSpecification",
          "dayOfWeek": "Monday",
          "opens": "18:30",
          "closes": "20:30"
        },
        {
          "@type": "OpeningHoursSpecification",
          "dayOfWeek": "Wednesday",
          "opens": "10:00",
          "closes": "12:00"
        }
      ],
      "location": [
        {
          "@type": "Place",
          "name": "Dojo zen de Lyon",
          "address": {
            "@type": "PostalAddress",
            "streetAddress": "11, rue Dumenge",
            "postalCode": "69004",
            "addressLocality": "Lyon",
            "addressCountry": "FR"
          }
        },
        {
          "@type": "Place",
          "name": "Boutique Miroirs du Ciel",
          "address": {
            "@type": "PostalAddress",
            "streetAddress": "9, rue de Belfort",
            "postalCode": "69004",
            "addressLocality": "Lyon",
            "addressCountry": "FR"
          }
        }
      ]
    })} />
    )}
    <slot name="head" />
  </head>
  <body class={isAdmin
    ? "bg-[#f0f0f1] text-gray-900 dark:bg-[#0c0d10] dark:text-gray-100 min-h-screen flex flex-col font-sans antialiased"
    : "bg-white text-gray-900 dark:bg-gray-900 dark:text-gray-100 min-h-screen flex flex-col font-sans antialiased"
  }>
    <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:top-3 focus:left-3 focus:z-50 focus:px-4 focus:py-2 focus:bg-gray-900 focus:text-white focus:rounded-lg focus:shadow-lg focus:outline-none">
      Aller au contenu principal
    </a>

    {isAdmin ? (
      <div class="admin-shell min-h-screen flex flex-col flex-1">
        <header class="sticky top-0 z-40 h-10 w-full bg-[#1d2327] dark:bg-[#111215] text-[#c3c4c7] border-b border-[#2c3338] dark:border-gray-800 px-3 sm:px-4 flex items-center justify-between text-xs select-none shadow-2xs">
          <div class="flex items-center gap-2 sm:gap-3 min-w-0">
            {hasAdminSession && (
              <button
                type="button"
                id="admin-mobile-toggle"
                class="md:hidden p-1.5 -ml-1 text-[#c3c4c7] hover:text-white rounded hover:bg-[#2c3338] transition cursor-pointer"
                aria-label="Afficher le menu d'administration"
                aria-expanded="false"
              >
                <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                </svg>
              </button>
            )}

            <a href="/admin" class="font-serif font-bold text-sm text-white tracking-tight hover:text-indigo-300 transition truncate flex items-center gap-1.5">
              <span>Ateliers de calligraphie</span>
            </a>

            <span class="text-[#50575e] hidden sm:inline select-none" aria-hidden="true">|</span>

            <a
              href="/"
              target="_blank"
              rel="noopener noreferrer"
              class="hidden sm:inline-flex items-center gap-1 text-[11px] text-[#c3c4c7] hover:text-white transition"
              title="Voir le site public (s'ouvre dans un nouvel onglet)"
            >
              <span>Voir le site</span>
              <span class="font-mono text-[10px]" aria-hidden="true">↗</span>
            </a>
          </div>

          <div class="flex items-center gap-2 sm:gap-3 shrink-0">
            <a
              href="/"
              target="_blank"
              rel="noopener noreferrer"
              class="sm:hidden inline-flex items-center gap-1 text-[11px] text-[#c3c4c7] hover:text-white transition"
            >
              <span>Voir le site</span>
              <span class="font-mono text-[10px]" aria-hidden="true">↗</span>
            </a>

            <button
              type="button"
              id="theme-toggle"
              class="p-1 rounded text-[#c3c4c7] hover:text-white hover:bg-[#2c3338] transition cursor-pointer"
              aria-label="Basculer le mode sombre"
              title="Basculer le mode sombre"
            >
              <svg id="icon-sun" class="w-3.5 h-3.5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              <svg id="icon-moon" class="w-3.5 h-3.5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
              </svg>
            </button>

            {hasAdminSession && (
              <button
                id="admin-logout-btn"
                type="button"
                class="text-[11px] text-[#c3c4c7] hover:text-rose-400 px-2 py-0.5 rounded hover:bg-[#2c3338] transition cursor-pointer"
              >
                Se déconnecter
              </button>
            )}
          </div>
        </header>

        {hasAdminSession ? (
          <div class="flex-1 flex min-h-[calc(100vh-2.5rem)] relative">
            <div
              id="admin-sidebar-overlay"
              class="fixed inset-0 bg-black/60 z-40 md:hidden hidden backdrop-blur-xs transition-opacity"
              aria-hidden="true"
            ></div>

            <aside
              id="admin-sidebar"
              class="fixed inset-y-0 left-0 z-50 md:static w-56 bg-[#1d2327] dark:bg-[#111215] text-[#c3c4c7] border-r border-[#2c3338] dark:border-gray-800 flex flex-col shrink-0 transform -translate-x-full md:translate-x-0 transition-transform duration-200 ease-in-out md:min-h-[calc(100vh-2.5rem)] md:sticky md:top-10 md:self-start md:h-[calc(100vh-2.5rem)] overflow-y-auto select-none"
              aria-label="Navigation de l'administration"
            >
              <div class="px-4 py-3.5 border-b border-[#2c3338] dark:border-gray-800 flex items-center justify-between">
                <div>
                  <div class="text-[10px] uppercase font-mono tracking-widest text-indigo-400 font-semibold">Administration</div>
                  <div class="font-serif font-bold text-sm text-white tracking-tight leading-tight">Ateliers de calligraphie</div>
                </div>
                <button
                  id="admin-sidebar-close"
                  type="button"
                  class="md:hidden text-gray-400 hover:text-white p-1 rounded cursor-pointer"
                  aria-label="Fermer le menu"
                >
                  <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                  </svg>
                </button>
              </div>

              <nav class="flex-1 py-2">
                <ul class="space-y-0.5 text-xs font-medium">
                  {adminNavItems.map((item) => {
                    const isActive = currentAdminTab === item.id;
                    return (
                      <li>
                        <a
                          href={item.href}
                          aria-current={isActive ? "page" : undefined}
                          class={isActive
                            ? "flex items-center gap-2.5 px-3.5 py-2 bg-indigo-600 dark:bg-indigo-700 text-white font-semibold border-l-4 border-white dark:border-indigo-300 transition-colors"
                            : "flex items-center gap-2.5 px-3.5 py-2 text-[#c3c4c7] hover:text-white hover:bg-[#2c3338] dark:hover:bg-gray-800 border-l-4 border-transparent transition-colors"
                          }
                        >
                          <span class="shrink-0 text-current" set:html={item.svg} />
                          <span class="truncate">{item.label}</span>
                        </a>
                      </li>
                    );
                  })}
                </ul>
              </nav>

              <div class="p-3 border-t border-[#2c3338] dark:border-gray-800 text-[11px] text-gray-500 font-mono text-center">
                Ateliers de calligraphie · CMS
              </div>
            </aside>

            <main id="main-content" class="flex-1 min-w-0 bg-[#f0f0f1] dark:bg-[#0c0d10] p-4 sm:p-6 lg:p-8 flex flex-col">
              <div class="flex-1 min-w-0">
                <slot />
              </div>

              <footer class="mt-12 pt-4 border-t border-gray-300 dark:border-gray-800 text-xs text-gray-500 dark:text-gray-400 flex flex-col sm:flex-row items-center justify-between gap-2">
                <div>Merci de faire confiance à <strong class="font-medium text-gray-700 dark:text-gray-300">Ateliers de calligraphie</strong>.</div>
                <div class="flex items-center gap-3">
                  <span class="text-gray-400 dark:text-gray-600">Espace d'administration</span>
                  <span class="text-gray-300 dark:text-gray-700 select-none">·</span>
                  <a href="/" target="_blank" rel="noopener noreferrer" class="hover:text-indigo-600 dark:hover:text-indigo-400 transition">Voir le site public ↗</a>
                </div>
              </footer>
            </main>
          </div>
        ) : (
          <main id="main-content" class="flex-1 flex items-center justify-center p-4 min-h-[calc(100vh-2.5rem)] bg-[#f0f0f1] dark:bg-[#0c0d10]">
            <slot />
          </main>
        )}
      </div>
    ) : (
      <Navbar />
    )}

    {!isAdmin && (
      showSidebar ? (
        <main id="main-content" class="flex-1 mx-auto max-w-[1300px] w-full flex flex-col lg:flex-row gap-8 px-4 sm:px-6 py-8">
          <div class="flex-1 min-w-0">
            <slot />
          </div>
          <aside class="lg:w-80 shrink-0">
            <InfoBox />
          </aside>
        </main>
      ) : (
        <main id="main-content" class="flex-1 mx-auto max-w-[1300px] w-full px-4 sm:px-6 py-8">
          <slot />
        </main>
      )
    )}

    {!isAdmin && <Footer />}

    <script>
      function syncIcon() {
        const isDark = document.documentElement.classList.contains('dark');
        const sun = document.getElementById('icon-sun');
        const moon = document.getElementById('icon-moon');
        if (sun) sun.classList.toggle('hidden', !isDark);
        if (moon) moon.classList.toggle('hidden', isDark);
      }
      syncIcon();

      document.getElementById('theme-toggle')?.addEventListener('click', () => {
        const isDark = document.documentElement.classList.toggle('dark');
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
        syncIcon();
      });

      document.getElementById('admin-logout-btn')?.addEventListener('click', async () => {
        await fetch('/api/admin/auth', { method: 'DELETE' }).catch(() => {});
        window.location.href = '/admin';
      });

      const mobileToggle = document.getElementById('admin-mobile-toggle');
      const sidebar = document.getElementById('admin-sidebar');
      const overlay = document.getElementById('admin-sidebar-overlay');
      const sidebarClose = document.getElementById('admin-sidebar-close');

      function openSidebar() {
        sidebar?.classList.remove('-translate-x-full');
        overlay?.classList.remove('hidden');
        mobileToggle?.setAttribute('aria-expanded', 'true');
      }

      function closeSidebar() {
        sidebar?.classList.add('-translate-x-full');
        overlay?.classList.add('hidden');
        mobileToggle?.setAttribute('aria-expanded', 'false');
      }

      mobileToggle?.addEventListener('click', () => {
        const isClosed = sidebar?.classList.contains('-translate-x-full');
        if (isClosed) openSidebar();
        else closeSidebar();
      });

      overlay?.addEventListener('click', closeSidebar);
      sidebarClose?.addEventListener('click', closeSidebar);
    </script>
  </body>
</html>
