---
// Le <main> du layout fournit déjà la grille éditoriale (.wrap, ~1200px) :
// ce conteneur ne rajoute donc pas de marges internes.
const { class: className } = Astro.props;
---

<div class:list={["w-full", className]}>
  <slot />
</div>
